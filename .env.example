# Copy to .env and fill in real values. .env is gitignored.
#
# JWT signing key for the .NET API (docker-compose passes it as Jwt__Key).
# Generate a strong one, e.g.:  openssl rand -base64 48
JWT_KEY=
